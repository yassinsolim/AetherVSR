# M10.5 stabilization scope

Starting clean main/origin/main:
`acb50f8e69aa2893e221d654edb7d22e7876636e`.
Separate npm ci/typecheck/lint/test/build/build:extension and full Python gates
passed: 629 Vitest tests (33 files), 308 Python tests. Exact-SHA CI
34813805398 completed/success, gate and fusion. Two existing moderate npm
advisories remain. Production payload is 263008 bytes, SHA256
`724507ec7d3e7a8c9a4ce7bf0fc772f2d2de9f56c1218693db52d6cba195258a`;
model SHA256
`d76fae7a295cdcdaecb44e39f8c87ff68a59ca1e07fc7cfc347d252d3cad358a`.

M10 remains EXTENSION MVP PARTIAL under its registered methodology. Its valid
600-second combined loss was 4.8011814863%; GPU cost alone is not a supported
cause. No retrospective metric change or promotion of M10 is permitted.

Priority: audit loss counters, add diagnostic accounting/scheduling observation,
then controlled M9/M10 and matched-page repeated comparisons plus at least two
new 600-second extension-neural replication runs before runtime behavior changes.
Report every valid trial and retain invalid raw data with reason/hash. Installation,
inactive injection/discovery, baseline and neural arms share a fixture. Equal
viewport/source/placement/foreground/power and balanced ordering are controls,
not properties to infer from historical files.

Initial falsifiable hypothesis: rVFC gaps and playback-quality dropped counters
are not proven disjoint frame populations. Check specification and the tested
Chrome implementation, then a bounded native callback-stall probe retaining
media time, rVFC metadata, quality deltas and successful submission sequence.
Browser-exposed timestamps/cumulative counters are not unique frame identities.
Reject the hypothesis if source/behavior establishes disjoint, aligned counters;
do not select a metric merely because it produces a smaller value.

This is scope registration, NOT final delivery-metric preregistration. After
the counter audit and independent review, commit M10.5-PREREGISTRATION.md before
final acceptance. If overlap cannot be quantified, retain the historical combined
metric as binding. Final acceptance must require repeated independent 600-second
passes and sustained late-window delivery, with strict focus/visibility integrity.

Keep model, WGSL, precision, graph, ingest and controller thresholds fixed during
attribution. No FPS-triggered fallback without measured baseline benefit. No M11,
training, temporal processing, interpolation, HDR, native backend or store release.

Separately diagnose generic Plyr/Video.js geometry and whole-package reload.
Minimized geometry fixtures and diagnosis precede any generic implementation;
no site whitelists, privileged MAIN bridge or permission expansion. Preserve
service-worker restart and original-video ownership/security behavior. Rerun
parity (3 external + 3 copy), 20 cleanup cycles, ownership and relevant journeys.

Independent reviews cover counter semantics/metric, paired methodology and
worktree fidelity, scheduling/CPU attribution, geometry, public compatibility,
reload, final acceptance/parity and verdict. Binding P0/P1 conclusions require
affected reruns. Final report has the requested 25 sections and exact closing
CI/clean-clone/publication proof; remaining gates remain explicit.

Starting tracked tree: 50270076 bytes; cap 50331648 bytes (48 MiB), headroom
61572 bytes. New long/Chrome raw traces remain local; commit compact summaries,
hashes and recipes only. No cap change, silent evidence deletion or history
rewrite. Stop for owner approval if legitimate source/tests/docs exceed the cap.