# M10.6 prospective public-identity correction

This owner-approved amendment supersedes only the cross-session `currentSrc`
digest rule in [the preregistration](M10.6-PREREGISTRATION.md). The original
[metadata pin](../results/m106-public-identity.json) remains unchanged.

The first public ordinal stopped before playback observation or any scheduled
action: its `currentSrc` digest differed from the pin. Two subsequent fresh,
paused metadata-only checks found distinct `blob:` URLs but the same HTTPS HLS
master URL digest and four rendition-path digests. A blob URL is a session-local
MediaSource handle, not a reusable asset identifier. Master manifest body hashes
also differed; signed rendition request query values changed. No playback outcome
was collected in those checks. The original preflight and diagnosis stay local,
with hashes retained in the replacement reference record.

Before restarting ordinal 1, commit the reviewed replacement reference and
apparatus. Compare these observations in all twelve fresh sessions:

- Exactly one observed unsigned HTTPS HLS master URL, with matching origin and
  SHA256 digest. The actual browser response must be HTTP200.
- Exactly the reference set of signed HLS rendition **path** SHA256 digests.
  Query values and CDN hostnames are recorded only as digests/origins and are
  not cross-session identity requirements. No unknown rendition or observer
  overflow may pass; failed manifest responses fail identity qualification.
- Original page origin, blob scheme, metadata dimensions and the already frozen
  duration tolerance. The full blob URL digest remains constant **within** each
  observation; source replacement still fails integrity.

Observe only requests made by the original page. Do not fetch alternative media,
change ABR/source selection, parse or retain public frames, override CORS/EME,
or persist URL paths, signed query values or other media credentials. This test
instrumentation adds no production dependency, permission or source-selection
behavior. The same response observer is present in P/Q/R/S.

These fingerprints identify a stable streaming entry point and observed rendition
paths, not cryptographic equality of every media byte. The original M10.5 asset
had no retained stable master identifier, so a historical asset match remains
unverified. Conclusions concern the freshly pinned public asset and tested browser.

No seek target, clock origin, action, repetition, ordering, stall threshold,
severity/recovery rule or readiness gate changes. Restart the complete fixed
P Q S R / Q R P S / S P R Q sequence; the original failed preflight contributes
no outcome. Primary native-floor data and Case D are unaffected. M10 and M10.5
remain PARTIAL. This is an apparatus correction, not a readiness ADR.

## Full-catalog qualification supersedes preload-only matching

The owner subsequently approved this second identity-only correction. The first
180012ms native window under the rule above retained the same blob and master,
but the original player naturally changed from960x540 to2560x1440 and requested
an additional rendition. Its closing preload-only identity check failed. That
attempt also recorded a failed scheduled pause/resume and a later genuine-ended
replay. Preserve the entire attempt, including its failed action; do not present
it as a pass or mix it with the restarted paired study.

The cross-session identity is now the **complete advertised master catalog**,
not the subset a paused player happened to preload. `m3u8-parser`7.2.0 reads only
the master response already requested by the page, with a128KiB body bound and
5s read deadline. Record a canonical SHA256 over all advertised rendition paths,
resolution, bandwidth, frame rate and codecs, including audio/subtitle URI paths
when advertised. Query values and CDN hosts remain excluded from that canonical
identity. Raw manifest text, media bytes and credentials are never persisted.

Require the exact same advertised catalog and master fingerprint in every
session. Every observed child-manifest path must belong to it. Original ABR
changes within the catalog are allowed and recorded; unknown renditions,
failed manifest responses and overflow still fail qualification. Dimensions at
opening/closing must be advertised, not necessarily equal. Blob identity and
source continuity within each session remain mandatory. No ABR choice is forced.

Also record the original video's dimensions and duration **at the prescribed
seek**. The four arms in each block must have matching seek-time dimensions and
the unchanged duration tolerance before the paired causal verdict can qualify.
Mismatch yields UNRESOLVED; it does not authorize discarding a valid trial or
repeating it to obtain a favorable rendition. Record dimensions throughout the
common sampler so subsequent original ABR transitions remain inspectable.

Commit a new metadata-only advertised-catalog pin and this reviewed apparatus
before restarting all twelve trials. Both previous attempts and pins remain
retained. All other scheduling, stall, recovery, safety and readiness rules are
unchanged. Full-catalog identity is still not immutable media-byte identity or
proof of the historical M10.5 asset. No primary-floor measurement is affected.

## Closing duration is an observed media outcome

The owner approved one further narrow classification correction after a third
retained native attempt. That180017.1ms window preserved the full catalog and
blob, but its MSE duration changed from35.95354 to35.925333 seconds. Opening and
seek-time duration met the frozen tolerance; closing duration did not. Its
fullscreen-exit timeout also remains a failed action, not a passing journey.

Keep duration qualification unchanged at opening and at the prescribed seek,
including within-block duration spread<=0.01s. At closing, require the unchanged
master/catalog/blob and advertised dimensions, but record duration evolution as
an outcome, not an asset-identity replacement. The common sampler now includes
duration alongside time/readiness/dimensions; unavailable duration remains null.
Stall proximity still uses the original35.963044 reference and0.15s tolerance.
No new seek, recovery, timing or readiness rule is introduced.

Restart all twelve observations only after this correction is committed. Keep
all three prior attempts and their action failures in the final evidence record.
For fullscreen exit, the runner prefers the player's original visible Exit
Fullscreen button; Escape remains the allowed alternative when none exists.
Neither action changes original playback state through a script or adds a
production site workaround. Failure to exit remains a failed required action.

## Fullscreen completion handshake

The next batch retained four complete journeys and one interrupted ordinal5.
A resize arrived3.7ms after the harness ended its fullscreen action, immediately
following other commanded fullscreen-exit resizes. Its strict integrity listener
classified that late native-window event as external. Keep the complete batch,
including first-block active presentation-check failures, outside the restarted
batch; no failed journey is promoted or silently replaced.

The harness now awaits restoration of the exact pre-fullscreen viewport and
200ms without resize, bounded by3s, before marking the commanded exit complete.
This is an operation-completion handshake, not a blanket resize/blur exemption.
Blur/pagehide/visibility checks remain active even during it; no focus repair,
window movement or playback rescue is introduced. Entry time and2s fullscreen
hold remain unchanged. Freeze the correction before a fresh complete sequence.