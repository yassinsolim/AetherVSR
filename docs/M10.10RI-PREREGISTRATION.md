# M10.10RI - Audio-Clock-Driven Timing Instrument Qualification

Status: prospective instrument protocol, before any new native observations.
This is the one authorized finite instrument investigation. No replay product
qualification, authority handover, Return, permissions, product lifecycle, neural,
soak, public census, capture or overlay experiment may follow in this milestone.
M10.11 is not authorized.

## 1. Starting Pin, Baseline and Budget

Separate required commands verified `/Users/ysoli/Projects/AetherVSR`, clean `main`,
HEAD==origin/main==`f0c3c49fd6e01746ebc232850455da14ac347c63`, and the twelve-commit
history. Starting CI35100333935 passed both jobs including research static checks.
Separate npm ci/typecheck/lint/check:m1010r/test/build/build:extension all passed.
Vitest:47 files,1,337 passed,3 opt-in skips. Complete Python:497 passed. Both
FFmpeg flags with player fixtures/replay timing:40 passed. Dependencies and install
policy remain frozen; two moderate advisories and existing install warnings remain.

Production274,684bytes SHA256 bundle
`46cebd53b7665ce48772792d2ec2eb73057e915d93ef76374900f30a159ac551`;
diagnostic276,414bytes SHA256 bundle
`7334c5db0f476048a10b63a2e0d9e4b240436fa3a669925e113cedc93cb1003f`;
model140,467bytes SHA256
`d76fae7a295cdcdaecb44e39f8c87ff68a59ca1e07fc7cfc347d252d3cad358a`.
The model is not loaded by the instrument.

Starting tracked54,183,335bytes, cap54,525,952, headroom342,617. Pre-edit planning
estimates (not measurements): source70,000; tests45,000; docs35,000; compact
results85,000. Total235,000; projected total54,418,335 and reserve107,617. Stop
for owner approval before exceeding the cap; preserve all historical evidence.
Raw PCM, media, block arrays, browser recordings/profiles remain below ignored
`.cache/m1010r/`. No new cache ignore or production dependency is needed.

## 2. Frozen Failure and Hypothesis

M10.10 remains INCOMPLETE RESEARCH CHECKPOINT / NO CONTROLLED WEB PRODUCT PATH
QUALIFIED. M10.10R remains CONTROLLED REPLAY PATH NOT QUALIFIED. Preserve exactly
one attempted `calibration-30-1`, five NOT_RUN suffixes, native frames[0,7168),
7,168 finite zero PCM samples, first scheduled signal12000,180 unverified windows,
null maximum sample error and null digital uncertainty. All product gates remain
NOT_RUN. Preserve the failed raw JSON SHA256
`a80e452ca7d45cbb0abe50f1e985bbd128656f0393feb9cde871fd22ad83b1bf` and PCM SHA256
`18619b678a5c207a971a0aa931604f48162e307c57ecdec450d5f095fe9f32c7`.

Hypothesis to falsify: a worklet can preserve required data and authorize completion
only after its actual render-clock sample range crosses a predeclared terminal
frame, independently of host scheduling. Wall time is a failure guard, not evidence
that scheduled samples rendered. A running AudioContext is not progress evidence.

## 3. Implementation and Browser Pins

The source pin for every native run is the exact clean committed implementation
whose tree contains this preregistration and the independently reviewed recorder,
host, analyzer and build. The runner must record that40-character SHA, refuse a
dirty tree or stale build, hash every package input/output, and bind the checkpoint
to that source/browser/media/analyzer identity. A final clean-pin record is written
before native launch; a self-referential future commit hash is not invented here.
No implementation or acceptance changes after the first observation; no retry.

Browser fixed to Chrome153.0.8010.12 with executable SHA256
`8319963f6625accf51c0dd4f55091ceaf9f09ed39e7a52fed4fae12b2a6b668a`.
Verify exact build/hash before running. Record actual machine/OS, command line,
default sink, context state, sample rate and visibility. No permission/chooser
interaction, autoplay override or undocumented browser switch. Physical refresh
and speaker/scanout latency are not measured.

## 4. Completion Authority and Block Semantics

Audio rate48,000 samples/s. Before constructing or connecting the recorder, send
an immutable integer `targetEndFrame` via processor options. Only execution of
`process()` may return RENDER_TARGET_REACHED after a contiguous block satisfies
`currentFrame + observedBlockLength >= targetEndFrame`. Frames are half-open:
recorded range[firstFrame,actualObservedEndFrame); the requested terminal frame is
exclusive. Retain the WHOLE final block, not a fabricated sample-exact truncation.
Record every actual block length and the exact requested/actual endpoints.

Acceptance requires firstFrame at/before first required signal/window; sum of
block lengths == processedSamples == actualEnd-first; the target inside the last
retained block; overshoot>=0 and<lastBlockLength. No permanent128-sample assumption.
Validate channel shape before copying. Zero-input blocks may contain real rendered
silence and are recorded as such; silence is not proof of waveform occurrence.
No discontinuity, regression, overflow, malformed result or nonfinite PCM is allowed.
Preallocated80s PCM/block storage; no per-quantum allocation of recording buffers.

Terminal states: RENDER_TARGET_REACHED, WATCHDOG_ABORT, AUDIO_CONTEXT_SUSPENDED,
PROCESSOR_ERROR, DISCONTINUITY, OVERFLOW, HOST_ABORT, MALFORMED_RESULT. Only the first
is eligible. Generic early `finish` is HOST_ABORT; no alternate success path.
Terminal state and buffers are emitted once. A later host abort cannot rewrite an
already render-qualified terminal; receipt does not itself prove full qualification.

## 5. Heartbeats, Watchdog and Partial Evidence

Worklet heartbeats are diagnostic: currentFrame, end frame, sampleRate, processed
blocks/samples, current block length, state and ordinal. First retained block then
at least12,000 render frames between heartbeats (nominal4Hz, block-rounded, not exact
delivery rate). Record every block length separately without messaging each quantum.
The terminal message replaces any heartbeat due on the last block; it carries the
final progress counts. Both host and offline cadence validation use that rule.
Host captures receive-clock and AudioContext.currentTime brackets, never treating
arrival as emission or physical output. Include context transitions, including
interrupted when surfaced. Render-frame deltas are progression evidence.

Short-control watchdog15s; media watchdog75s. Both send only `snapshot-and-abort`
with WATCHDOG_ABORT and allow2s for partial drainage. If no worklet response arrives,
retain heartbeats/context/host events and mark missing PCM explicitly. No watchdog
can send successful finish or independently declare COMPLETE. Unexpected suspension,
processor error or host abort uses its own failure reason and bounded drainage.

Host handlers are installed before graph connection. If a render terminal was
already queued, retain its true state even when delivered after watchdog firing;
the watchdog-fired flag remains a failed-run condition, never erased by late data.
Collector cancellation propagates into the short control and waits for its bounded
partial-data drainage before closing AudioContext resources; it cannot abandon
the control promise and discard completed or partial buffers.
Mocks must also show intact successful recording when host delivery is delayed but
no watchdog failure occurs. Snapshot abort retains actual partial samples/block
history, requested/actual endpoints, flags and heartbeat summary.

## 6. Signal Geometry and Offline Verification

Reuse the existing independently decoded70s48kHz PCM and MP4 assets from M10.10R:
30fps MP4 SHA256 `e006f3d5381d73b1ca5739f5e74216f4bf0c312f28621634d258a770822a8f9b`;
60fps MP4 SHA256 `5171a8b6da7303c8c409167f4191b7330f41120fcd89d7e3aa0df9483b237b29`.
Their full video identities/PTS, audio PTS continuity and actual positive/negative
audio-only AAC shifts remain ground truth, not new native calibration.

Short control: let F=ceil(AudioContext.currentTime*48000) before scheduling, solely
to choose a future epoch. firstScheduledFrame=F+12000. Three regions begin at
first+0, first+26400, first+45600, each8192 samples, reference offsets48000/96000/
144000 and gains1/0.5/0.75. Final tail4800 samples. Target=first+58592=F+70592.
The first signal remains later than150ms, deliberately excluding old7168-frame
coverage. Context time selects scheduling, but never authorizes completion.

All180 fixed interior256-sample windows must independently appear in returned PCM.
Search+/-24 samples; normalized squared error<=1e-6 and exactly one match, absolute
location error<=12 samples. Record the observed maximum, including true observed
zero when applicable, separately from uncertainty. Verify finite PCM, continuous
block ranges and at least4800 tail samples. Offline validation rechecks samples,
not just worklet success metadata. Negative controls deliberately omit, move,
duplicate or corrupt signals. Full all-start dense matching uses the previously
reviewed512-sample/128-stride detector and independent4096-sample global waveform
cross-checks; never infer a bound solely from the first accepted offset.

## 7. Baseline Media Timing Window

Only after that run's short control passes may its baseline media observation start.
No model, neural WGSL, RuntimeController or product authority code enters the package.
Reuse baseline pipeline and same-imported-texture identity observer; read GPU
diagnostics only after stopping, never per frame. No production source edit.

Before media play let M=ceil(AudioContext.currentTime*48000). Immutable timing window
[M+240000,M+3120000), exactly60s of audio render frames following5s warmup. Recorder
target=M+3216000 (67s from M), providing2s audio tail. Keep all observations, select
the window by recorded audio brackets, not message-arrival time. Media is70s and
non-looping. Missing first/last anchors fails rather than shifting the window.

Record full available rVFC metadata, element time, actual successful submission
brackets, decoded texture identity, sequence/generation and queue-completion upper
endpoints. A public pipeline count, GPU record count and callback count certify
serialized completeness. GPU completion callback time is not exact GPU finish or
scanout. It is an observed upper endpoint that may widen the interval.

## 8. Cross-Clock and Uncertainty Model

Heartbeat/completion cross-check residual is host AudioContext.currentTime minus
the recorded worklet endFrame/48000, with both host read brackets retained. Positive
residual includes asynchronous delivery and further rendering, not clock error or
speaker latency. No finite positive delivery bound is inferred. It must be finite,
nonnegative clocks must be monotonic, and negative residual may not exceed two
observed maximum block durations (conservative double block-rounding guard, tested
in mocks, not a latency claim). An unexplained greater regression stops the run.
Context time never replaces worklet progress as completion authority.

The claimed uncertainty is only a finite-fixture conditional digital interval for
identified frame-start PTS minus the audio render-frontier media timeline. Existing
enclosing anchors already include their observed spacing and the+/-12-sample
correspondence allowance. Expand each side by observed maximum audio block duration
plus1/48000s sample quantization, the full same-task main-clock bracket (<=2ms),
and one verified video container PTS tick (1/15360s). The actual frame identity
must be valid; ambiguity is not a bounded extra term. Show all components without
double counting anchor spacing or waveform error. Report width and half-width.

Prospective maximum total HALF-width15ms for every accepted planned timing row,
at least99% valid coverage, no extrapolation/discontinuity/ambiguous identity,
complete sequence boundaries, and fully supported first/last20s render windows.
Every bound remains conditional on validated finite-fixture content correspondence,
contiguous monotonic clocks and these explicit granularity enclosures. Unsupported
conversion or missing component yields null, never a0ms aggregate bound. Speaker,
scanout, finish-to-photon and human-perceived latency remain unbounded/not measured.

## 9. Mock Gates and Native Budget

Before native: host-clock-ahead/audio stalled; audio-clock-ahead/host servicing held;
bounded host stall; suspension/processor failure; manual early finish; old7168
point; exact/spanning boundaries for block lengths1/64/96/128/192/256/512; overflow,
shape errors, sequence/heartbeat inconsistencies, delayed abort and waveform
corruption. Tests execute bundled worklet/host code with independently advanced
clocks, not a fixture that synthesizes all audio only when host finish arrives.
Only native results can qualify the instrument.

ONE native sequence, fixed order30,60,30,60 with new IDs `ri-30-1`, `ri-60-1`,
`ri-30-2`, `ri-60-2`. Each is a fresh page/context with short control followed by
baseline media instrument observation. No separate pilot/retry/native suspension
experiment is budgeted. Stop immediately on first failed target, waveform, metadata,
clock relation, cleanup or quantitative gate. Remaining IDs become NOT_RUN.
Never reuse M10.10R IDs or alter their artifacts.

If all four pass, report all per-run bounds, actual completion overshoots and
heartbeat progress; both repeats of each FPS must have median digital phase spread
<=one source frame, with actual first/last20s drift<=one frame. No pooled run may
hide an individual failure. Check each same-FPS pair as soon as its second member
completes; a failed30fps pair at run three prohibits run four. Missing phase or
unverified prior analysis also stops, rather than deferring acceptance until after
all runs. No pooled run may
hide an individual failure. Initial audio-clock warmup and host delivery delays
are separately observed, never thermal/performance claims. Instrument overhead is
the observed records/messages/buffers and software/GPU timing scope; no unmeasured
counterfactual processing cost is invented.

## 10. Review, Verdict and Closure

Independent pre-native review covers completion authority, quantum boundaries,
watchdog, partial snapshots, heartbeat rate, waveform verification, uncertainty,
and first-run stop. Resolve all P0/P1 before the source pin/native launch. Independent
post-run review verifies raw hashes/reasons/ranges/waveforms, repeatability,
cross-clock interpretation, bound and verdict. Any affected P0/P1 invalidates
measurement; no automatic re-run or instrument revision is permitted.

Exactly one verdict: DIGITAL TIMING INSTRUMENT QUALIFIED; DIGITAL TIMING INSTRUMENT
NOT QUALIFIED; or DIGITAL TIMING INSTRUMENT UNRESOLVED. This is not a new replay
product verdict. Only a complete instrument PASS may recommend a separate future
replay prerequisite study; do not execute it here or call it M10.11 implementation.
Otherwise the finite investigation is exhausted: request an explicit direction
decision between a scientifically valid weaker claim, cooperating-site/API,
native desktop or discontinuing browser qualification. No RI.2 by default.

Final29-section report, compact hashes/results, all separate baseline/final gates,
failed-artifact preservation, frozen payload checks, size guard and independent
clean clone are required. Push only known-good closure; verify clean HEAD==origin/main
and both exact-final-SHA CI jobs including research static checks. Raw artifacts
stay ignored and are required to reproduce native measurements, not to test a clone.