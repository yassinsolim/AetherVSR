# M12.1 Portable Native Conformance Runner & Physical Hardware Completion

## 1. Starting State
M12.1 started from clean `main` at M12 closure `50296ac780b8db5b70a0b443bdd8380eef5372bd`, synchronized with origin, with exact CI `35323718577` passing gate and fusion.

## 2. Frozen M12 Result
M12 remains permanently `CROSS-VENDOR DESKTOP PARTIAL`. Its Apple `UNRESOLVED`, NVIDIA/AMD/Intel `NOT_RUN_HARDWARE_UNAVAILABLE` states and report are unchanged.

## 3. Native Golden-Runner Design
The runner uses shared `verifyGolden` and the production WGSL builders/model packing path. It executes f32 and available f16 graph paths, captures stem, each body checkpoint and final output, and compares against committed trusted vectors.

## 4. Golden-Runner Falsification Tests
Focused tests reject wrong model identity, wrong tensor/stage identity, nonfinite values, failed stages, failed output and software/fallback adapter evidence. Normal builds exclude the golden asset and diagnostic entrypoint.

## 5. Numeric Tolerance Policy
Existing `verifyGolden` tolerances remain unchanged: f32 `1e-3`, f16 `5e-2`, and final output at least `1.5/255`. No tolerance was changed after Apple results.

## 6. Portable Windows Runner
`npm run m12:validate-machine -- <output>` records environment, model/media/source pins, adapter placeholder, stage-golden result, staged later-gate states, artifact hashes and a final envelope. It is portable Node/Electron workflow scaffolding and fails closed; no Windows hardware was available.

## 7. Binding Source Pin
The M12.1 evidence source is `acfb596002a01796961905959c7a66b7afa162c1`. Runtime/model/security/product files remain unchanged from M12; subsequent commits are apparatus/report-only.

## 8. Apple Environment
Mac17,2, Apple M5, macOS 26.6.2 build 25G83, arm64, Electron 44.4.1, Chromium 152.0.7977.78, local graphical session. Display refresh was not newly measured by the stage runner.

## 9. Apple Golden Result
PASS for f32 and f16. Adapter vendor is Apple, `fallbackAdapter=false`, shader-f16 and timestamp-query are present. Stem, body.0, body.1 and final output comparisons passed. The production model file SHA is `d76fae...d358a`; the embedded ModelFile identity is `9154d9...8e02`.

## 10. Apple Overall Result
`PASS`. The native stage-golden gate is resolved; same-machine parity, short controls and lifecycle also pass, and unchanged M11 long-run evidence remains applicable.

## 11. NVIDIA Environment
No qualifying physical Windows NVIDIA RTX machine was available. No software or CI adapter was substituted.

## 12. NVIDIA Golden/Parity
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 13. NVIDIA Short Result
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 14. NVIDIA Lifecycle
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 15. NVIDIA Long Result
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 16. AMD Environment
No qualifying physical Windows AMD Radeon machine was available. No software or CI adapter was substituted.

## 17. AMD Golden/Parity
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 18. AMD Short Result
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 19. AMD Lifecycle
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 20. AMD Long Result
`NOT_RUN_HARDWARE_UNAVAILABLE`.

## 21. Intel Status
No qualifying modern physical Intel Xe/Arc machine was available. Intel remains `NOT_RUN_HARDWARE_UNAVAILABLE`.

## 22. Cross-Vendor Numeric Comparison
No cross-vendor numeric comparison is possible. Apple stage results pass within preregistered tolerances; no fp16 bit-identity assumption was made.

## 23. Import-Path Matrix
Apple external and forced-copy smoke/parity paths pass. NVIDIA, AMD and Intel are not run.

## 24. GPU Timing
No new vendor ranking or theoretical efficiency claim was introduced. Existing M11 Apple GPU timing remains descriptive evidence only.

## 25. Cadence and Visual Age
The unchanged Apple M11 short matrix passes all eight arms and its long neural/raw replacement evidence remains applicable because production runtime/model behavior did not change. No Windows cadence or age evidence exists.

## 26. Generic Portability Findings
No generic portability defect was found. The missing native stage runner was an evidence gap, not a product failure.

## 27. Per-Machine Verdicts
Apple: `PASS`. NVIDIA: `NOT_RUN_HARDWARE_UNAVAILABLE`. AMD: `NOT_RUN_HARDWARE_UNAVAILABLE`. Intel: `NOT_RUN_HARDWARE_UNAVAILABLE`.

## 28. Overall M12.1 Verdict
**CROSS-VENDOR COMPLETION PARTIAL**

Apple native conformance is resolved, but the required NVIDIA, AMD and Intel physical classes remain unavailable.

## 29. Public Claim Scope
AetherVSR Desktop's Apple M5 720p30/60 WebGPU neural path has native stage-golden, parity, short-performance and lifecycle evidence. No NVIDIA, AMD or Intel support claim is made.

## 30. Repository, CI, and Size State
The compact envelope is [results/m12.1-envelope.json](../results/m12.1-envelope.json). M12.1 adds the shared runner, portable staged workflow, tests and docs only. The M11 56 MiB cap remains in force. Final M12.1 CI is required before publication.

## 31. Recommended Next Milestone
Do not begin M13. The bounded next step is to run the portable workflow on real Windows NVIDIA and AMD hardware, then Intel if available. Do not optimize until a physical compatibility failure is measured.
