# M10.10R - Controlled Replay Timing, Authority and Audio Handover

**CONTROLLED REPLAY PATH NOT QUALIFIED**

Research stopped at its first native instrument calibration. This is a negative
qualification result, not evidence that progressive replay is impossible. The
scheduled-audio recorder ended before its render clock reached any test signal.
No candidate timing, authority handover, Return, neural playback or soak was run.
The measured apparatus has a known completion-clock defect and is not qualified
for reuse. No native retry or next-milestone implementation follows from this report.

[Prospective protocol](M10.10R-PREREGISTRATION.md) and
[compact evidence](../results/m10.10r-replay.json) distinguish native observations,
offline controls, platform mocks, derived diagnosis and NOT_RUN gates. All raw
artifacts remain ignored; the index verifies their hashes without rewriting them.

## 1. Starting State

Separate commands established clean `main`, HEAD==origin/main==
`5b1a313e1d603b46471aebb70594e044a67d44d6` in `/Users/ysoli/Projects/AetherVSR`.
The required twelve-commit history was recorded. Starting exact-SHA CI35086643662
passed `gate` and `fusion`.

Fresh separate baseline gates: npm ci/typecheck/lint/test/build/build:extension
passed; Vitest45 files,1,249 passed,2 opt-in skips; FFmpeg player fixtures8 passed;
complete Python311 passed. Node26.5.0. The two existing moderate advisories, ESLint
deprecation and pending install-script warnings remain; dependencies are frozen.

Frozen production274,684bytes / bundle SHA256
`46cebd53b7665ce48772792d2ec2eb73057e915d93ef76374900f30a159ac551`;
diagnostic276,414bytes / bundle SHA256
`7334c5db0f476048a10b63a2e0d9e4b240436fa3a669925e113cedc93cb1003f`;
model140,467bytes / SHA256
`d76fae7a295cdcdaecb44e39f8c87ff68a59ca1e07fc7cfc347d252d3cad358a`.
Initial tracked53,855,518bytes, cap54,525,952, headroom670,434. The pre-edit495,000
additional-byte estimate fit with175,434 projected reserve; estimates are not
final tree measurements.

## 2. Frozen M10.10 Result

M10.10 remains **INCOMPLETE RESEARCH CHECKPOINT / NO CONTROLLED WEB PRODUCT PATH
QUALIFIED**, with12/12 P0 controlled-surface parity, scoped progressive replay/
refetch acquisition, exact paused-input comparisons and denial/grant/revocation
evidence. R2 remains unqualified, R3/R6 unresolved, R4/R5 unsupported-safe only in
their exact tested topology. Candidate timing, A/V,720p60 neural and public census
remain NOT_RUN. None was repeated, relabeled or silently promoted by this study.
The old compact index still has SHA256
`b7d110addf560efae427d160331ff012d4613ce2a21d38e86e2199d952d3920d`.

## 3. Replay Product Scope

Only R1 progressive direct replay/selected-resource optional-host refetch under
PLAYER_AUTHORITY was considered. Proposed supported classes were packaged/local,
CORS progressive HTTP(S), and explicitly authorized progressive refetch. Complete
owned bytes and a streaming URL have different independence guarantees.

The prospective claim was a supported-progressive-video player, never arbitrary
web video. Blob/MSE reconstruction, HLS/DASH reverse engineering, live/srcObject,
DRM and all capture routes were excluded. No generic overlay, model retraining,
controller tuning, production permission or store change occurred. M11 stays gated.

## 4. Static Research-Code Coverage

`npm run check:m1010r` now uses strict TypeScript and existing typed ESLint rules
through a dedicated project. All12 reused/new qualification TypeScript files pass
with zero errors/warnings, including the formerly excluded isolated player. Chrome
types are explicit. Historical player setup retains its Promise API with explicit
completed promises; no new native player qualification is inferred from this edit.

The unused historical capture target is explicitly excluded and absent from the
calibration package. Global rules were not weakened. CI has a separate research
static-check step. JavaScript orchestration and Python analysis have runnable
regressions; they are not mislabeled TypeScript coverage. Production source is
unchanged. Protocol/static commit: `155d4cf3d1a6dd8fbef5f27c3a941fe767fcfc87`.

## 5. Timing-Media Ground Truth

New deterministic70s H.264/AAC fixtures have16-bit video identities, complements
and guards, known30/60fps, deterministic sample-addressed noise and70 impulses.
Every decoded video frame matched its identity and integer container PTS:2,100
at30fps and4,200 at60fps. Both decode to3,360,000 mono48kHz PCM samples with a
verified first decoded PTS of zero and complete contiguous PTS coverage. Zero is
observed here, not assumed for arbitrary media.

| Asset | Bytes | SHA256 |
| --- | ---: | --- |
|30fps MP4 |1,076,460 |`e006f3d5381d73b1ca5739f5e74216f4bf0c312f28621634d258a770822a8f9b` |
|60fps MP4 |1,211,083 |`5171a8b6da7303c8c409167f4191b7330f41120fcd89d7e3aa0df9483b237b29` |

Real audio-only AAC stream-copy shifts of-50ms/+50ms at both rates preserved video
PTS and exact8192-sample interior waveforms at shifted audio media times. Negative
trimming and positive priming remain visible. Full-length shift validation is
53,398bytes, SHA256 `a16d2e5d625f3411b09819ad528da3d743a76c9478831cd19885803e9c9734b9`.
This validates container-origin handling, not speaker onset. An earlier offline
media directory was incomplete after an unreliable terminal completion; it is
retained, not treated as a validated asset or native attempt. Final assets came
from a fresh directory and passed full decoding.

## 6. Timing Instrument

The designed quantity was identified frame-start media PTS relative to the audio
render-frontier media timeline at successful submission. Same-texture GPU identity
recording was to correlate actual rendering input with rVFC metadata and sequence/
generation. Queue completion gives a software-ready upper endpoint, not scanout or
GPU elapsed duration. No per-frame CPU pixel readback or awaited import/submit gap
was introduced. The probe was NOT configured or exercised by the stopped native run.

Audio design uses actual AudioWorklet `currentFrame`, recorded PCM and48kHz sample
positions. Dense globally unique fingerprint matches, full-window correlation,
independent global waveform controls and enclosing anchors avoid sparse-marker
interpolation claims. MediaElementSource -> gain -> recorder -> output was the
intended research product graph, but it was never created in the native attempt.
Only the preliminary AudioBufferSource controls ran. Graph equivalence remains
NOT_RUN; no native-element playback result can substitute for it.

## 7. Instrument Calibration/Bounds

Measured source: `90dc459d2a144f201b7a84e77e9eff8aebbb7b6c`, clean package bundle
SHA256 `9e3eb8a5f68d315480538d7e1e15328751091490bad411066d930aeb0db6ad3a`.
Environment: Apple M5 / Mac17,2,24GiB, macOS26.6.2 build25G83, Chrome153.0.8010.12;
browser executable SHA256
`8319963f6625accf51c0dd4f55091ceaf9f09ed39e7a52fed4fae12b2a6b668a`.
Physical display refresh was not measured. Normal autoplay policy, temporary
profile, no permission override/chooser. GPU acquisition reported timestamp-query
available; capability availability is not a measured processing time.

Fixed order was30,60,30,60,30,60, each5s warmup plus60s observation. Only
`calibration-30-1` was attempted; it ended UNRESOLVED before any media observation.
Five suffix records are NOT_RUN. Six terminal IDs are not six experiments.

The control recorded56 blocks of128 samples,7,168 samples total, frames[0,7168).
All28,672 bytes of retained Float32 PCM are finite zero samples, SHA256
`18619b678a5c207a971a0aa931604f48162e307c57ecdec450d5f095fe9f32c7`.
The scheduled signal ranges were[12000,20192),[38400,46592),[57600,65792).
The recording overlaps none. Its149.333ms of sample coverage is not a wall-clock
duration, clock-rate measurement, audio gap or speaker latency.

All180 scheduled windows were unverified; zero were measured successfully and
maximum sample error is null. Independent offline recomputation agreed. The host
timer sent `finish` before actual render sample progress reached those signals.
The implementation assumed clock progress when scheduling completion from a wall
timer. Output-device warmup, browser scheduling and OS causation remain hypotheses,
not established root causes. A `running` AudioContext does not prove progression.

Primary native error: `Scheduled audio-clock calibration failed`. Media PCM was
then absent; `Audio recording is not finished` was a secondary extraction error.
The immutable saved analyzer says missing PCM, while this report separately gives
the more specific post-run diagnosis. Nothing rewrites the original artifact.
Digital uncertainty bound, physical latency and calibration repeatability are
**not measured**. No candidate timing-acceptance addendum was earned or committed.

## 8. Playback-Authority State Machine

Design-only: SOURCE_ACTIVE -> PREPARING_REPLAY -> HANDOVER -> PLAYER_ACTIVE ->
RETURNING -> SOURCE_ACTIVE, with ABORTED/RECOVERY_REQUIRED. Exclusive exact-source
leases, transaction/document/generation fencing and independent playback versus
remote-capability lifetimes were preregistered. They were not implemented or
natively qualified after the calibration stop. Old M10.10 PLAYER_AUTHORITY labels
do not prove this transactional contract.

## 9. Enter Handover

NOT_RUN. The design prepared muted replay, validated the selected source, paused
only that source, captured its final media time, reconciled and started the player
behind a closed gate, then enabled audio. Independent review identified the need
for source-local exclusive leases and idempotent phase acknowledgments. Those
requirements remain unverified, not a shipped handover implementation.

## 10. Audio Overlap/Gap

NOT_RUN. Prospective targets were zero normal-operation gate overlap and a250ms
upper bound on the explicitly browser-observable handover gap. No source audible
stop or player audible start was observed. The silent calibration PCM does NOT
represent successful muting, a handover gap, or zero double-audio. Listening was
not used as a substitute for instrumentation.

## 11. Return to Page

NOT_RUN. Registered semantics: stop player authority/audio, revalidate the exact
original source, seek it, restore original page audio/rate, and resume iff it was
playing at enter. Return would remain paused if originally paused. Player-local
audio changes would not overwrite the original page state. Replaced sources must
disable Return. This is a future contract, not behavior proved here.

## 12. Failure/Rollback

Product rollback NOT_RUN. Before-pause failures were to leave the page untouched;
after-pause failures required bounded exact-source rollback or explicit recovery.
Direct Close was prospectively defined to leave the original source paused, with
no guessed automatic return. No source-page pause or takeover occurred in this
study, so setup cleanup cannot qualify those paths.

Native attempted-setup cleanup recorded zero device/pipeline/probe/audio-node/
observer/timer/pending-completion resources, closed AudioContext and paused video;
the page and research browser were closed. No owner action remains. A later
test-only regression independently advances host/audio clocks and reproduces the
early-finish refusal. The measured timer implementation was NOT repaired or rerun;
its known defect remains explicit. Mocked cleanup tests are not native rollback.

## 13. Permission/Refetch

New binding10-row matrix NOT_RUN, including both rates, optional-host/authenticated
fixtures, denied/revoked permission, and both redirect cases. The calibration
package requested no permissions or host grants and had no privileged broker,
content scripts or page-accessible resource bridge. No privileged refetch occurred.
Historical R1 evidence remains scoped to its old apparatus.

Prospective semantics distinguish committed owned bytes from revocable fetch/
Return authority: revocation must reject new fetch, not automatically erase
already-owned media. Credential and redirect policy, revocation races and byte
ownership still require native qualification in any future replay implementation.

## 14. Source Replacement

NOT_RUN. The design invalidates remote Return/control for removed/replaced videos,
same-element src changes, navigation or tab closure, while complete owned replay
may continue. No command may attach to replacement media. M10.10's post-measurement
mocked invalidation repair was not natively validated by this study.

## 15. Worker Restart

NOT_RUN. The proposed distinction between retired broker capabilities and continued
owned playback was never exercised. The permission-free calibration worker was
loaded normally; its existence is not a worker-restart safety test. Source leases
and stale epochs still require explicit native testing before qualification.

## 16. Multiple Player Sessions

NOT_RUN. Same-source contenders were to be rejected without disturbing the first
lease. Separate-source players were to remain independently tracked when the older
player closed. No second acquisition player was created. Old mocked regression
coverage is retained, not relabeled native evidence.

## 17. Input Provenance

The final-apparatus three-timestamps-by-two-classes regression was NOT_RUN.
Offline all-frame media validation is not source-page/replay pixel parity. Existing
M10.10 exact Canvas2D/GPU-copy comparisons remain unchanged and do not prove that
the unimplemented authority/audio changes preserve provenance.

## 18. Raw 30fps Cadence

NOT_RUN. No first submission occurred, no warmup or60s observation opened, and
there were zero callbacks. Zero is the observed callback count, NOT measured0fps.
No loop/seek/stall distribution or useful-frame rate exists for this attempt.

## 19. Raw 60fps Cadence

NOT_RUN. The first60fps calibration and all candidate60fps paths were gated after
the initial30fps instrument failure. No extrapolation from old10s windows or30fps
ground truth is made. The unchanged prospective raw gate remains58 useful FPS
overall and final20s for60fps media.

## 20. A/V Sync

NOT_RUN. No decoded-media audio/render-video pair was collected. The designed
digital metric concerns render-frontier media time, not actual speaker/scanout.
Neither the prospective12-sample correspondence allowance nor the10ms interval
half-width limit was empirically earned. Missing bounds are null, not zero.

## 21. Drift

NOT_RUN. There are no first/middle/final media windows or cross-repeat phases.
The stopped control's sample coverage cannot be extrapolated to steady-state
clock rate, drift or a user's playback experience.

## 22. Seek Recovery

NOT_RUN. Forward/backward seek, reconvergence, old-frame generation rejection and
source-page timeline invariance remain unverified. No recovery threshold was
selected from candidate data because none was collected.

## 23. Pause/Resume

NOT_RUN. The calibration's final cleanup pause is not the registered pause/resume
journey and does not establish audio/video reconvergence or authority invariance.

## 24. Playback-Rate Behavior

NOT_RUN at1.0x and1.25x for the product. The detector assumes a constant1x epoch;
it must not certify resampled/rate-changed media without new prospective coverage.
No non-1.0x support or safe-refusal behavior is claimed.

## 25. Neural Qualification Eligibility

Ineligible. The instrument did not calibrate, and provenance regression, raw
cadence, A/V, handover, Return, lifecycle and permission prerequisites were not
earned. All were required, not optional. The failed early calibration was not
replaced, normalized away, or bypassed with another acquisition candidate.

## 26. 720p60 Neural Result

NOT_RUN. No C16D2 model or RuntimeController was included in the calibration
package; production weights/shaders/controller remain unchanged. No submitted/
rendered FPS, GPU p50/p95, fallback or final-window claim exists. The58-FPS neural
expectation and per-run acceptance were not relaxed. Catmull appeared only as the
unexercised planned calibration renderer, not a measured cost or quality control.

## 27. Long Soak

NOT_RUN. The conditional600s neural soak was not eligible. No thermal, sustained
performance, long-window audio, resource-growth or A/V claim is made.

## 28. Security/Reviewer Findings

Independent reviewers examined the design, implementations, controls and retained
failure. Pre-native findings corrected: sparse-anchor assumptions, decoded PTS
origin controls, absolute sample-clock controls, boundary completeness, hidden
intervals without callbacks, early fractional timer completion, disposal races,
and completion without raw checkpoint evidence. These fixes preceded the measured
pin. The host/audio completion-clock defect was found only after native failure;
it remains a P1 prerequisite for future apparatus reuse, not a hidden measured pass.

A P2 lexical-only cache containment risk remains in the standalone media producer;
actual retained paths were checked and are not symlinked. Build/checkpoint readers
reject symlinks. This requires hardening before reuse on untrusted output paths,
not a retrospective change to generated evidence. Production/source scope passed
independent review; no new privileged page-facing fetch/response path was found.

| Required Review Area | Review/Evidence Disposition |
| --- | --- |
|1. Timing apparatus design | Independent changes requested, corrected before first native attempt |
|2. Timing calibration | Independent native failure audit; bound not earned |
|3. Audio detector | Offline controls reviewed; native sample coverage independently recomputed |
|4. Authority state machine | Design reviewed; implementation/native NOT_RUN |
|5. Enter handover | Design races reviewed; implementation/native NOT_RUN |
|6. Return to page | Exact-owner/rollback design reviewed; implementation/native NOT_RUN |
|7. Permission/refetch | Historical policy and new scope reviewed; new native matrix NOT_RUN |
|8. Worker restart | Prospective lifetime distinction reviewed; native NOT_RUN |
|9. Generation invalidation | Prospective stale-owner prohibition reviewed; native NOT_RUN |
|10. Multiple sessions | Exclusive-owner/tracking design reviewed; native NOT_RUN |
|11. Cadence | Counter/foreground completeness reviewed; candidate rate NOT_RUN |
|12. Neural qualification | Independent stop/eligibility review; ineligible, NOT_RUN |
|13. Security | Calibration isolation/frozen scope reviewed; product security NOT_RUN |
|14. Evidence/report | Raw hashes/analysis independently checked; final report review recorded at publication |
|15. Final decision | Timing reviewer recommended NOT QUALIFIED; supervising decision adopts it |

These are coverage areas across independent reviews, not15 successful native tests
or15 distinct reviewers. Some reviewer environments lacked extension-provided
generic review criteria; direct code/evidence reviews were performed and no claim
is made that unavailable tooling ran. Required product behaviors remain unverified.

## 29. Product Verdict

**CONTROLLED REPLAY PATH NOT QUALIFIED.** The complete supported-progressive-media
contract was not demonstrated. One reviewer considered PARTIAL defensible as a
research-progress description; the final decision uses NOT QUALIFIED to avoid
implying an implemented or qualified product subset. The exact blocker is an
audio-control recording that ended before scheduled sample-frame evidence.
No browser-impossibility, cooperating-site requirement or capture pivot is inferred.

## 30. Repository/CI/Size State

The measured apparatus commit90dc459 tracked54,117,827bytes, leaving408,125bytes.
Its pre-native gates passed: standard typecheck/lint, strict research checks,
default Vitest1,335 passed/3 opt-in skips across47 files, complete Python497 passed,
and39 combined opt-in media/checkpoint tests. These are source checks, not product
qualification. Later changes are a failure regression, compact reporting and docs.
Final separate local gates on that closure worktree:

| Gate | Observed Result |
| --- | --- |
| `npm ci` | PASS; existing advisory/deprecation/install warnings retained |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS |
| `npm run check:m1010r` |12 qualification TypeScript files, zero errors/warnings |
| `npm run test` |47 files,1,337 passed,3 opt-in skips |
| `npm run build` | PASS |
| `npm run build:extension` | PASS |
| Complete `tools/test_*.py` suite |497 passed |
| Both FFmpeg flags, player fixtures and replay timing tests |40 passed, none skipped |
| Saved timing analysis | Reproduced exactly; remains UNRESOLVED |
| Authority/lifecycle evidence | NOT_RUN declarations and prerequisite stop verified, not behavioral PASS |
| Compact report reproduction | PASS, all referenced artifact hashes verified |
| Independent final evidence/decision review | PASS for negative closure; known apparatus defect retained |

Every production/diagnostic payload byte matches the frozen starting controls.
Two builds of each also matched including provenance; the initial local check
truthfully records90dc459 plus pending closure edits. No new calibrated timing
result is implied. The local evidence audit is401bytes, SHA256
`693a2ba1465a9c47d30440aa3bde8a177f00094b69de06eed01450b5278ab19a`.
Clean-clone own installation, repeated clean builds, final committed-tree size,
remote synchronization and exact-final-HEAD CI remain publication checks.

The compact index initially reproduces at23,538bytes, SHA256
`c2d87b03e4c12314e03975cb1c0dbbab8003ce464f8b15e3d45fffbefb8ad7d1`.
No raw media/PCM/screenshots/profiles were committed, historical evidence was not
deleted, and the52MiB cap was not raised. Native raw remains necessary to reproduce
the index; a clean clone can validate code/tests but does not contain recordings.
Historical parallel parity timeouts remain distinct from replay performance; no
test deadline was raised to turn them green. Final exact-HEAD CI and clean remote
synchronization are publication gates, not implied by starting CI or local tests.

## 31. Recommended Next Milestone

Do not begin M10.11. Recommend at most **one separately authorized replay-instrument
investigation**: terminate recording on verified AudioWorklet sample-frame progress,
using a wall-clock watchdog only to preserve partial evidence and fail safely.
Preregister independent host/audio-clock stall tests and clock-driven completion
controls before new native data. Retain this attempted ID and all five NOT_RUN
records; any new evidence needs a new source/experiment pin and independent review.

This is a narrowly identified instrument issue, not a reason to demand cooperating
sites or move to native desktop yet. If a bounded digital instrument still cannot
be established under that finite investigation, reassess the product direction
explicitly. No automatic browser permutation, overlay/capture loop, new permission
action, production implementation or next-milestone code is authorized here.